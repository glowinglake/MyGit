## mindb-codex

`mindb-codex` is a small educational SQL database written in Python.

### Run

From the repo root:

```bash
cd mindb-codex
python -m mindb_codex --db ./_mindb_codex_db
```

### REPL basics

- End statements with `;`
- Meta commands:
  - `.help`
  - `.quit`


